<!DOCTYPE html>
<html lang="<%= lang %>" dir="<%= lang === 'ar' ? 'rtl' : 'ltr' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t.community.title %> - <%= t.siteName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <% if (lang === 'ar') { %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <% } %>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Noto+Kufi+Arabic:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --syria-green: #007A3D;
            --bg-dark: #0a0a0a;
            --bg-card: #111111;
            --text-primary: #ffffff;
            --text-secondary: #a0a0a0;
        }
        body { background: var(--bg-dark); color: var(--text-primary); font-family: 'Inter', sans-serif; }
        .navbar { background: rgba(10, 10, 10, 0.95); border-bottom: 1px solid #222; }
        .navbar-brand { font-weight: 700; font-size: 1.5rem; color: white !important; }
        .nav-link { color: var(--text-secondary) !important; }
        .nav-link:hover, .nav-link.active { color: var(--syria-green) !important; }
        .btn-lang { background: transparent; border: 1px solid #333; color: var(--text-secondary); padding: 0.25rem 0.75rem; border-radius: 20px; }
        .page-header { padding: 120px 0 60px; text-align: center; }
        .page-title { font-size: 3rem; font-weight: 700; }
        .page-subtitle { color: var(--text-secondary); font-size: 1.25rem; }
        .community-card { background: var(--bg-card); border: 1px solid #222; border-radius: 12px; padding: 2rem; text-align: center; height: 100%; transition: all 0.3s; }
        .community-card:hover { border-color: var(--syria-green); transform: translateY(-5px); }
        .community-icon { font-size: 3rem; margin-bottom: 1rem; }
        .community-title { font-size: 1.5rem; font-weight: 600; margin-bottom: 0.5rem; }
        .community-desc { color: var(--text-secondary); }
        .btn-join { background: var(--syria-green); border: none; padding: 0.5rem 1.5rem; border-radius: 8px; color: white; margin-top: 1rem; display: inline-block; text-decoration: none; }
        .btn-join:hover { background: #009245; color: white; }
        .footer { padding: 40px 0; background: #050505; border-top: 1px solid #222; text-align: center; color: var(--text-secondary); }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/?lang=<%= lang %>">🇸🇾 <%= t.siteName %></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item"><a class="nav-link" href="/?lang=<%= lang %>"><%= t.nav.home %></a></li>
                    <li class="nav-item"><a class="nav-link" href="/download?lang=<%= lang %>"><%= t.nav.download %></a></li>
                    <li class="nav-item"><a class="nav-link" href="https://explorer.opensyria.net?lang=<%= lang %>" target="_blank"><%= t.nav.explorer %></a></li>
                    <li class="nav-item"><a class="nav-link active" href="/community?lang=<%= lang %>"><%= t.nav.community %></a></li>
                    <li class="nav-item"><a class="nav-link" href="/docs?lang=<%= lang %>"><%= t.nav.docs %></a></li>
                </ul>
                <div class="d-flex gap-2">
                    <a href="?lang=en" class="btn btn-lang">EN</a>
                    <a href="?lang=ar" class="btn btn-lang">عربي</a>
                </div>
            </div>
        </div>
    </nav>

    <section class="page-header">
        <div class="container">
            <h1 class="page-title"><%= t.community.title %></h1>
            <p class="page-subtitle"><%= t.community.subtitle %></p>
        </div>
    </section>

    <section class="pb-5">
        <div class="container">
            <div class="row g-4 justify-content-center">
                <% t.community.channels.forEach(function(channel) { %>
                <div class="col-md-6 col-lg-3">
                    <div class="community-card">
                        <div class="community-icon"><%= channel.icon %></div>
                        <h3 class="community-title"><%= channel.name %></h3>
                        <p class="community-desc"><%= channel.description %></p>
                        <% if (channel.link !== '#') { %>
                        <a href="<%= channel.link %>" class="btn-join" target="_blank"><%= lang === 'ar' ? 'انضم' : 'Join' %></a>
                        <% } else { %>
                        <span class="btn-join" style="opacity: 0.5; cursor: not-allowed;"><%= lang === 'ar' ? 'قريباً' : 'Coming Soon' %></span>
                        <% } %>
                    </div>
                </div>
                <% }); %>
            </div>
        </div>
    </section>

    <footer class="footer">
        <p><%= t.footer.copyright %></p>
        <p class="mt-2" style="color: #007A3D; font-size: 1.25rem;"><%= t.footer.freesyria %> 🇸🇾</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
