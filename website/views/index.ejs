<!DOCTYPE html>
<html lang="<%= lang %>" dir="<%= lang === 'ar' ? 'rtl' : 'ltr' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t.siteName %> - <%= t.tagline %></title>
    <meta name="description" content="<%= t.hero.description %>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <% if (lang === 'ar') { %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <% } %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Noto+Kufi+Arabic:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --syria-red: #CE1126;
            --syria-white: #FFFFFF;
            --syria-black: #000000;
            --syria-green: #007A3D;
            --bg-dark: #0a0a0a;
            --bg-card: #111111;
            --text-primary: #ffffff;
            --text-secondary: #a0a0a0;
        }
        
        * {
            font-family: <%= lang === 'ar' ? "'Noto Kufi Arabic'" : "'Inter'" %>, sans-serif;
        }
        
        body {
            background: var(--bg-dark);
            color: var(--text-primary);
            min-height: 100vh;
        }
        
        /* Navbar */
        .navbar {
            background: rgba(10, 10, 10, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid #222;
        }
        
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
        }
        
        .nav-link {
            color: var(--text-secondary) !important;
            transition: color 0.3s;
        }
        
        .nav-link:hover, .nav-link.active {
            color: var(--syria-green) !important;
        }
        
        .btn-lang {
            background: transparent;
            border: 1px solid #333;
            color: var(--text-secondary);
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.875rem;
        }
        
        .btn-lang:hover {
            background: #222;
            color: white;
        }
        
        /* Hero */
        .hero {
            padding: 120px 0 80px;
            background: linear-gradient(135deg, var(--bg-dark) 0%, #0d1a0d 100%);
            position: relative;
            overflow: hidden;
        }
        
        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--syria-green), var(--syria-white), var(--syria-red));
        }
        
        .hero-title {
            font-size: 4rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .hero-subtitle {
            font-size: 1.5rem;
            color: var(--syria-green);
            margin-bottom: 1rem;
        }
        
        .hero-description {
            font-size: 1.25rem;
            color: var(--text-secondary);
            max-width: 600px;
            margin: 0 auto 1.5rem;
        }
        
        .genesis-quote {
            font-style: italic;
            color: var(--text-secondary);
            font-size: 1rem;
            margin-top: 2rem;
            padding: 1rem;
            border-<%= lang === 'ar' ? 'right' : 'left' %>: 3px solid var(--syria-green);
        }
        
        .btn-primary-custom {
            background: var(--syria-green);
            border: none;
            padding: 0.75rem 2rem;
            font-weight: 600;
            border-radius: 8px;
            transition: all 0.3s;
        }
        
        .btn-primary-custom:hover {
            background: #009245;
            transform: translateY(-2px);
        }
        
        .btn-outline-custom {
            background: transparent;
            border: 2px solid var(--syria-green);
            color: var(--syria-green);
            padding: 0.75rem 2rem;
            font-weight: 600;
            border-radius: 8px;
            transition: all 0.3s;
        }
        
        .btn-outline-custom:hover {
            background: var(--syria-green);
            color: white;
        }
        
        /* Stats */
        .stats-section {
            padding: 60px 0;
            background: var(--bg-card);
        }
        
        .stat-card {
            text-align: center;
            padding: 2rem;
        }
        
        .stat-value {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--syria-green);
        }
        
        .stat-label {
            color: var(--text-secondary);
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        /* Features */
        .features-section {
            padding: 80px 0;
        }
        
        .section-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .section-subtitle {
            color: var(--text-secondary);
            margin-bottom: 3rem;
        }
        
        .feature-card {
            background: var(--bg-card);
            border: 1px solid #222;
            border-radius: 12px;
            padding: 2rem;
            height: 100%;
            transition: all 0.3s;
        }
        
        .feature-card:hover {
            border-color: var(--syria-green);
            transform: translateY(-5px);
        }
        
        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }
        
        .feature-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        .feature-description {
            color: var(--text-secondary);
            font-size: 0.95rem;
        }
        
        /* Specs */
        .specs-section {
            padding: 80px 0;
            background: var(--bg-card);
        }
        
        .spec-table {
            background: var(--bg-dark);
            border-radius: 12px;
            overflow: hidden;
        }
        
        .spec-row {
            display: flex;
            justify-content: space-between;
            padding: 1rem 1.5rem;
            border-bottom: 1px solid #222;
        }
        
        .spec-row:last-child {
            border-bottom: none;
        }
        
        .spec-label {
            color: var(--text-secondary);
        }
        
        .spec-value {
            font-weight: 500;
            color: var(--syria-green);
        }
        
        /* Quick Start */
        .quickstart-section {
            padding: 80px 0;
        }
        
        .code-block {
            background: #1a1a1a;
            border: 1px solid #333;
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 1rem;
            font-family: 'Monaco', 'Menlo', monospace;
            font-size: 0.875rem;
            overflow-x: auto;
        }
        
        .code-step {
            color: var(--syria-green);
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        /* Footer */
        .footer {
            padding: 60px 0 30px;
            background: #050505;
            border-top: 1px solid #222;
        }
        
        .footer-title {
            font-weight: 600;
            margin-bottom: 1rem;
        }
        
        .footer-link {
            color: var(--text-secondary);
            text-decoration: none;
            display: block;
            margin-bottom: 0.5rem;
            transition: color 0.3s;
        }
        
        .footer-link:hover {
            color: var(--syria-green);
        }
        
        .footer-bottom {
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid #222;
            text-align: center;
            color: var(--text-secondary);
        }
        
        .syria-free {
            font-size: 1.5rem;
            color: var(--syria-green);
        }
        
        /* Flag animation */
        .flag-stripe {
            height: 4px;
            background: linear-gradient(90deg, 
                var(--syria-green) 0%, var(--syria-green) 33%,
                var(--syria-white) 33%, var(--syria-white) 66%,
                var(--syria-red) 66%, var(--syria-red) 100%
            );
        }
        
        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }
            .hero-subtitle {
                font-size: 1.25rem;
            }
            .stat-value {
                font-size: 1.75rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/?lang=<%= lang %>">
                🇸🇾 <%= t.siteName %>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'home' ? 'active' : '' %>" href="/?lang=<%= lang %>"><%= t.nav.home %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'download' ? 'active' : '' %>" href="/download?lang=<%= lang %>"><%= t.nav.download %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://explorer.opensy.net?lang=<%= lang %>" target="_blank"><%= t.nav.explorer %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'community' ? 'active' : '' %>" href="/community?lang=<%= lang %>"><%= t.nav.community %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'docs' ? 'active' : '' %>" href="/docs?lang=<%= lang %>"><%= t.nav.docs %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://github.com/opensy/OpenSY" target="_blank"><%= t.nav.github %></a>
                    </li>
                </ul>
                <div class="d-flex gap-2">
                    <a href="?lang=en" class="btn btn-lang <%= lang === 'en' ? 'active' : '' %>">EN</a>
                    <a href="?lang=ar" class="btn btn-lang <%= lang === 'ar' ? 'active' : '' %>">عربي</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero -->
    <section class="hero text-center">
        <div class="container">
            <h1 class="hero-title"><%= t.hero.title %></h1>
            <p class="hero-subtitle"><%= t.hero.subtitle %></p>
            <p class="hero-description"><%= t.hero.description %></p>
            <p class="text-success mb-4">✅ <%= t.hero.launchDate %></p>
            <div class="d-flex gap-3 justify-content-center flex-wrap">
                <a href="/download?lang=<%= lang %>" class="btn btn-primary-custom"><%= t.hero.downloadWallet %></a>
                <a href="https://explorer.opensy.net?lang=<%= lang %>" class="btn btn-outline-custom" target="_blank"><%= t.hero.viewExplorer %></a>
            </div>
            <div class="genesis-quote">
                <%= t.genesisQuote %><br>
                <small><%= t.genesisCaption %></small>
            </div>
        </div>
    </section>

    <!-- Stats -->
    <section class="stats-section">
        <div class="container">
            <div class="row">
                <div class="col-6 col-md-3">
                    <div class="stat-card">
                        <div class="stat-value" id="stat-blocks">52,000+</div>
                        <div class="stat-label"><%= t.stats.blocks %></div>
                    </div>
                </div>
                <div class="col-6 col-md-3">
                    <div class="stat-card">
                        <div class="stat-value">~500 MH/s</div>
                        <div class="stat-label"><%= t.stats.hashrate %></div>
                    </div>
                </div>
                <div class="col-6 col-md-3">
                    <div class="stat-card">
                        <div class="stat-value">12+</div>
                        <div class="stat-label"><%= t.stats.peers %></div>
                    </div>
                </div>
                <div class="col-6 col-md-3">
                    <div class="stat-card">
                        <div class="stat-value">520M+</div>
                        <div class="stat-label"><%= t.stats.supply %> SYL</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features -->
    <section class="features-section">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="section-title"><%= t.features.title %></h2>
                <p class="section-subtitle"><%= t.features.subtitle %></p>
            </div>
            <div class="row g-4">
                <% t.features.items.forEach(function(feature) { %>
                <div class="col-md-6 col-lg-4">
                    <div class="feature-card">
                        <div class="feature-icon"><%= feature.icon %></div>
                        <h3 class="feature-title"><%= feature.title %></h3>
                        <p class="feature-description"><%= feature.description %></p>
                    </div>
                </div>
                <% }); %>
            </div>
        </div>
    </section>

    <!-- Specs -->
    <section class="specs-section">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="section-title"><%= t.specs.title %></h2>
            </div>
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="spec-table">
                        <% t.specs.items.forEach(function(spec) { %>
                        <div class="spec-row">
                            <span class="spec-label"><%= spec.label %></span>
                            <span class="spec-value"><%= spec.value %></span>
                        </div>
                        <% }); %>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Quick Start -->
    <section class="quickstart-section">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="section-title"><%= t.quickStart.title %></h2>
                <p class="section-subtitle"><%= t.quickStart.subtitle %></p>
            </div>
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <% t.quickStart.steps.forEach(function(step, index) { %>
                    <div class="code-step"><%= (index + 1) %>. <%= step.title %></div>
                    <div class="code-block"><%= step.code %></div>
                    <% }); %>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4">
                    <h5 class="footer-title">🇸🇾 <%= t.siteName %></h5>
                    <p class="text-secondary"><%= t.tagline %></p>
                    <p class="syria-free"><%= t.footer.freesyria %> 🕊️</p>
                </div>
                <div class="col-md-2 mb-4">
                    <h6 class="footer-title"><%= t.footer.links.title %></h6>
                    <a href="https://github.com/opensy/OpenSY" class="footer-link"><%= t.footer.links.github %></a>
                    <a href="https://explorer.opensy.net" class="footer-link"><%= t.footer.links.explorer %></a>
                    <a href="/docs?lang=<%= lang %>" class="footer-link"><%= t.footer.links.docs %></a>
                </div>
                <div class="col-md-3 mb-4">
                    <h6 class="footer-title"><%= t.footer.resources.title %></h6>
                    <a href="https://github.com/opensy/OpenSY/blob/main/docs/WALLET_RESTORE_GUIDE.md" class="footer-link"><%= t.footer.resources.walletGuide %></a>
                    <a href="https://github.com/opensy/OpenSY/blob/main/docs/NODE_OPERATOR_GUIDE.md" class="footer-link"><%= t.footer.resources.nodeGuide %></a>
                </div>
                <div class="col-md-3 mb-4">
                    <h6 class="footer-title"><%= t.footer.community.title %></h6>
                    <a href="#" class="footer-link"><%= t.footer.community.twitter %></a>
                    <a href="#" class="footer-link"><%= t.footer.community.telegram %></a>
                    <a href="#" class="footer-link"><%= t.footer.community.discord %></a>
                </div>
            </div>
            <div class="footer-bottom">
                <p><%= t.footer.copyright %></p>
                <div class="flag-stripe mt-3"></div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
